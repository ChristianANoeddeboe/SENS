<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar.FullScreen" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:datePickerDialogTheme">@style/MyDatePickerDialogStyle</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- This changes the date picker dialog theme -->
    <style name="MyDatePickerDialogStyle" parent="@style/ThemeOverlay.AppCompat.Dialog">
        <item name="colorAccent">@color/sensBlue</item>
        <item name="colorControlActivated">@color/sensRedDarkerBright</item>
        <item name="colorControlHighlight">@color/sensRed</item>
        <item name="android:headerBackground">@color/sensBlue</item>
    </style>
    <!-- Seek bar stuff -->
    <style name="MySeekBar" parent="Widget.AppCompat.SeekBar">
        <item name="android:progressBackgroundTint">@color/white</item>
        <item name="android:progressBackgroundTintMode">@color/white</item>
        <item name="android:progressTint">@color/sensRedDarkerBright</item>
        <item name="android:colorControlActivated">@color/sensRed</item>
    </style>

    <!-- Buttons -->
    <style name="BlueButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/sensBlueBrighter</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="TextButtonSensBlue" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/sensBlueDarker</item>
        <item name="android:colorPressedHighlight">@color/sensBlueDarker</item>
        <item name="android:elevation">0dp</item>
    </style>

    <style name="TextButtonSensWhite" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/white</item>
        <item name="android:colorPressedHighlight">@color/sensBlueDarkerDark</item>
        <item name="android:elevation">0dp</item>
    </style>


    <!-- Text fields -->
    <style name="FilledTextFieldWhite" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/whiteHint</item>
        <item name="android:back">@color/white</item>
    </style>

    <style name="FilledTextFieldBlue" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="android:backgroundTint">@color/sensBlueDarker</item>
        <item name="android:textColor">@color/sensBlue</item>
    </style>
</resources>
